# Ingestion Service

Ingests competitor social posts. v1 uses a fixture-backed connector interface (no live scraping/paid API integration yet) so the rest of the pipeline can be built and tested against realistic data.

## API

- `GET /health` — liveness check.

Full endpoint set (accounts, posts, trigger-ingestion) lands in Phase 2; once implemented, the contract is also available as interactive docs at `/docs` (Swagger UI, auto-generated by FastAPI) when the service is running.

## Run standalone

```bash
cd services/ingestion
uv sync
cp .env.example .env
uv run uvicorn app.main:app --reload --port 8001
```

## Env vars

See [.env.example](.env.example). `DATABASE_URL` is the only one required to run standalone.

## Tests

```bash
uv run pytest
```
